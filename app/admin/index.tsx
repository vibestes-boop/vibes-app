import { AdminWebConsole } from './AdminWebConsole';

export default function AdminDashboard() {
  return (
    <AdminWebConsole
      title="Admin Companion"
      subtitle="Eine zentrale Konsole für App, Web und Mobile-Web."
      primaryPath="/command-center"
    />
  );
}
